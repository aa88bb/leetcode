class Solution {
public:
    int titleToNumber(string s) {
        int count = 0;
        for(int i=0;i<s.size();i++){
            count = count*26 + (s.at(i) - 'A'+1);
        }
        return count;
    }
};
